# EasyLine — исправление шапки 2.2.2-header-fix

Патч на основе опубликованной версии 2.2.2: верхняя служебная навигация перенесена в основную строку шапки. Отдельный верхний этаж удалён; мобильное меню сохранено без изменений.

Публичный сайт: https://moseskm98-code.github.io/easyline-site/

- Основа: локальный handoff-пакет `design_handoff_easyline_site`
- Дата обновления публикации: 30 августа 2026 года
- Фотографии блюд и производства являются сгенерированными визуализациями для прототипа.

Публикация сохраняет отдельный слой увеличенной типографики и принудительно светлое оформление для мобильных браузеров. Белая оболочка сайта следует реальной ширине окна и не оставляет голубую полосу справа на больших телефонах и широких экранах.

## Ветка исправленной версии

Исправление хранится в ветке `2.2.2-header-fix`, созданной от опубликованной `version-2.2.2`. Исходная версия 2.2.2 сохранена без изменений.

Контроль после развёртывания:

- в HTML сохранён исходный маркер `window.__EASYLINE_VARIANT__ = "2.2.2"`, чтобы правка шапки не переключала другие варианты шаблона;
- `frameMax` равен `100vw`;
- на ширине 440 px правая граница `.el-site-shell` совпадает с правой границей окна.

## Ветка дизайн-системы

Ветка `design-system` создана от опубликованного коммита `e9cc283` и содержит
отдельный живой каталог интерфейсных основ и компонентов в `design-system/`.
Публичные страницы и их runtime не изменяются.

Локальная проверка каталога:

```bash
/Users/user1/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node scripts/verify-design-system.mjs
```
